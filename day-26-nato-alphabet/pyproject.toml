[tool.poetry]
name = "day-26-nato-alphabet"
version = "1.0.0"
description = ""
authors = [""]

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
